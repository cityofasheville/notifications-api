{
  "name": "notifications-api",
  "version": "1.2.8",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "nodemon --exec node --experimental-modules src/local.server.js",
    "test": "cd test; sam local invoke --event sam_event.json",
    "clean": "rm -rf deploy/build && rm -rf deploy/.terraform && rm -rf deploy/.terraform.lock.hcl",
    "runterraform": "cd deploy/build && terraform init && terraform apply -auto-approve -var-file=../../.env",
    "deploy": "cd deploy && node deploy.js && npm install --prefix ./build/nodejs --omit-dev && npm run runterraform",
    "destroy": "cd deploy/build && terraform init && terraform destroy -auto-approve -var-file=../../.env"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/server": "^4.10.0",
    "@as-integrations/aws-lambda": "^3.1.0",
    "@aws-sdk/client-secrets-manager": "^3.525.0",
    "@aws-sdk/client-ses": "^3.507.0",
    "axios": "^1.6.7",
    "connect-pg-simple": "^9.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.1",
    "express": "^4.18.2",
    "express-session": "^1.18.0",
    "lru-cache": "^10.2.0",
    "memorystore": "^1.6.7",
    "node-jose": "^2.2.0",
    "pg": "^8.11.3",
    "pug": "^3.0.2",
    "qs": "^6.11.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
